@using ShopView.Areas.Admin.Models
@model AccountIndexViewModel

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Admin/</span> Accounts Management
    </h4>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }

    <!-- Filter Card -->
    <div class="card mb-4">
        <div class="card-body">
            <form method="get" asp-area="Admin" asp-controller="Account" asp-action="Index">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">SEARCH</label>
                        <input type="text" name="keyword" class="form-control" placeholder="Search by username, email, phone"
                               value="@Model.Filter.Keyword" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">ROLE</label>
                        <select name="role" class="form-select">
                            <option value="">Select Role</option>
                            <option value="admin" selected="@(Model.Filter.Role == "admin" ? "selected" : null)">Admin</option>
                            <option value="customer" selected="@(Model.Filter.Role == "customer" ? "selected" : null)">Customer</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">STATUS</label>
                        <select name="isActive" class="form-select">
                            <option value="">Select Status</option>
                            <option value="true" selected="@(Model.Filter.IsActive == true ? "selected" : null)">Active</option>
                            <option value="false" selected="@(Model.Filter.IsActive == false ? "selected" : null)">Inactive</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">SORT BY</label>
                        <select name="sortBy" class="form-select">
                            <option value="id" selected="@(Model.Filter.SortBy == "id" ? "selected" : null)">ID</option>
                            <option value="username" selected="@(Model.Filter.SortBy == "username" ? "selected" : null)">Username</option>
                            <option value="email" selected="@(Model.Filter.SortBy == "email" ? "selected" : null)">Email</option>
                            <option value="role" selected="@(Model.Filter.SortBy == "role" ? "selected" : null)">Role</option>
                            <option value="status" selected="@(Model.Filter.SortBy == "status" ? "selected" : null)">Status</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">SORT ORDER</label>
                        <select name="sortOrder" class="form-select">
                            <option value="asc" selected="@(Model.Filter.SortOrder == "asc" ? "selected" : null)">Ascending</option>
                            <option value="desc" selected="@(Model.Filter.SortOrder == "desc" ? "selected" : null)">Descending</option>
                        </select>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-12 d-flex justify-content-end gap-2">
                        <button type="submit" class="btn btn-primary">Filter</button>
                        <a asp-area="Admin" asp-controller="Account" asp-action="Index" class="btn btn-secondary">Clear</a>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Create Button -->
    <div class="text-end mb-4">
        <a href="/Admin/Account/Create" class="btn btn-primary">
            <i class="bx bx-plus me-1"></i> Create New User
        </a>
    </div>

    <!-- Accounts Table -->
    <div class="card">
        <h5 class="card-header">Account List</h5>
        <div class="table-responsive text-nowrap">
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>USERNAME</th>
                        <th>EMAIL</th>
                        <th>ROLE</th>
                        <th>STATUS</th>
                        <th>ACTIONS</th>
                    </tr>
                </thead>
                <tbody class="table-border-bottom-0">
                    @if (Model.PagedResult?.Data != null && Model.PagedResult.Data.Any())
                    {
                        @foreach (var account in Model.PagedResult.Data)
                        {
                            <tr>
                                <td>@account.UserID</td>
                                <td>@account.Username</td>
                                <td>@account.Email</td>
                                <td>
                                    <span class="badge @(account.Role == "admin" ? "bg-label-primary" : "bg-label-info")">
                                        @account.Role?.ToUpper()
                                    </span>
                                </td>
                                <td>
                                    <span class="badge @(account.IsActive ? "bg-label-success" : "bg-label-danger")">
                                        @(account.IsActive ? "ACTIVE" : "INACTIVE")
                                    </span>
                                </td>
                                <td>
                                    <div class="dropdown">
                                        <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                            <i class="bx bx-dots-vertical-rounded"></i>
                                        </button>
                                        <div class="dropdown-menu">
                                            <a class="dropdown-item" asp-action="Edit" asp-route-id="@account.UserID">
                                                <i class="bx bx-edit-alt me-1"></i> Edit
                                            </a>
                                            @if (account.IsActive)
                                            {
                                                <form asp-area="Admin" asp-controller="Account" asp-action="Deactivate" method="post" style="display:inline;">
                                                    <input type="hidden" name="id" value="@account.UserID" />
                                                    <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Are you sure you want to deactivate this account?');">
                                                        <i class="bx bx-trash me-1"></i> Deactivate
                                                    </button>
                                                </form>
                                            }
                                            else
                                            {
                                                <form asp-area="Admin" asp-controller="Account" asp-action="Activate" method="post" style="display:inline;">
                                                    <input type="hidden" name="id" value="@account.UserID" />
                                                    <button type="submit" class="dropdown-item text-success" onclick="return confirm('Are you sure you want to activate this account?');">
                                                        <i class="bx bx-check-circle me-1"></i> Activate
                                                    </button>
                                                </form>
                                            }
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center">No accounts found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.PagedResult.TotalPages > 1)
        {
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center mt-3">
                    @if (Model.PagedResult.CurrentPage > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="Index" 
                               asp-route-page="@(Model.PagedResult.CurrentPage - 1)"
                               asp-route-pageSize="@Model.Filter.pageSize"
                               asp-route-keyword="@Model.Filter.Keyword"
                               asp-route-isActive="@Model.Filter.IsActive"
                               asp-route-role="@Model.Filter.Role"
                               asp-route-sortBy="@Model.Filter.SortBy"
                               asp-route-sortOrder="@Model.Filter.SortOrder">Previous</a>
                        </li>
                    }

                    @for (int i = 1; i <= Model.PagedResult.TotalPages; i++)
                    {
                        <li class="page-item @(i == Model.PagedResult.CurrentPage ? "active" : "")">
                            <a class="page-link" asp-action="Index"
                               asp-route-page="@i"
                               asp-route-pageSize="@Model.Filter.pageSize"
                               asp-route-keyword="@Model.Filter.Keyword"
                               asp-route-isActive="@Model.Filter.IsActive"
                               asp-route-role="@Model.Filter.Role"
                               asp-route-sortBy="@Model.Filter.SortBy"
                               asp-route-sortOrder="@Model.Filter.SortOrder">@i</a>
                        </li>
                    }

                    @if (Model.PagedResult.CurrentPage < Model.PagedResult.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="Index"
                               asp-route-page="@(Model.PagedResult.CurrentPage + 1)"
                               asp-route-pageSize="@Model.Filter.pageSize"
                               asp-route-keyword="@Model.Filter.Keyword"
                               asp-route-isActive="@Model.Filter.IsActive"
                               asp-route-role="@Model.Filter.Role"
                               asp-route-sortBy="@Model.Filter.SortBy"
                               asp-route-sortOrder="@Model.Filter.SortOrder">Next</a>
                        </li>
                    }
                </ul>
            </nav>
        }
    </div>
</div>
